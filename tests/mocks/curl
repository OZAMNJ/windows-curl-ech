#!/usr/bin/env bash

# Minimal realistic curl mock
# Does not simulate network logic or version state.
# Just logs invocation and returns configurable exit code.

LOG_FILE="${BATS_TMPDIR:-/tmp}/curl_args.log"
echo "$@" >> "$LOG_FILE"

exit "${MOCK_EXIT_CODE:-0}"
